# Shared Web Development (`web-dev`)

Installs the shared web development toolchain used by shnri projects.

```jsonc
"features": {
  "ghcr.io/shnri/shared-devcontainer-features/web-dev:1": {}
}
```

See the [collection README](../../README.md) for included tools, options, persistence behavior, and release instructions.
